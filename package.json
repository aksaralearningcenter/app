{
  "name": "aksara-frontend",
  "version": "1.0.0",
  "private": true,
  "description": "Frontend statis Aksara Learning Center — landing page + panel admin (Vite)",
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "dev:web": "vite",
    "dev:api": "npm --prefix server run dev:local",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "eslint docs/src/js",
    "test": "node --test \"tests/**/*.test.mjs\""
  },
  "devDependencies": {
    "eslint": "^9.39.5",
    "vite": "^8.3.0"
  }
}
